{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install && npm install"
    },
    "deploy": {
        "preDeployCommand": "php artisan key:generate --force && php artisan migrate:fresh --force",
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT && npm run dev"
    }
}
